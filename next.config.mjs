/** @type {import('next').NextConfig} */
const nextConfig = {
  // Removed ignoreBuildErrors - we should fix TypeScript errors, not hide them
  // Removed unoptimized images - Next.js optimization improves performance
}

export default nextConfig
