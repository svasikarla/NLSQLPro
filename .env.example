# ================================
# NLSQL Pro - Environment Variables
# ================================

# ----------------
# REQUIRED
# ----------------

# LLM Provider Selection
# Options: "anthropic" | "azure-openai"
# Default: "anthropic"
LLM_PROVIDER=anthropic

# Anthropic API Key (Claude AI)
# Get your API key from: https://console.anthropic.com/
# Required if LLM_PROVIDER=anthropic
ANTHROPIC_API_KEY=sk-ant-your-api-key-here

# Supabase Configuration
# Get these from your Supabase project settings: https://app.supabase.com/
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# Supabase Service Role Key (for server-side operations)
# WARNING: Keep this secret! Never expose in client-side code
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# Encryption Key for Database Credentials
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# MUST be 64 hex characters (32 bytes)
ENCRYPTION_KEY=your-64-character-hex-encryption-key-here

# ----------------
# OPTIONAL
# ----------------

# Azure OpenAI Configuration
# Get these from your Azure OpenAI resource: https://portal.azure.com/
# Required if LLM_PROVIDER=azure-openai
# AZURE_OPENAI_API_KEY=your-azure-openai-api-key
# AZURE_OPENAI_ENDPOINT=https://your-resource-name.openai.azure.com/
# OR: https://your-resource-name.cognitiveservices.azure.com/
# AZURE_OPENAI_DEPLOYMENT_NAME=your-gpt4-deployment-name
# AZURE_OPENAI_API_VERSION=2024-02-15-preview

# OpenAI API Key (for multi-LLM support and fallback)
# Get your API key from: https://platform.openai.com/api-keys
# OPENAI_API_KEY=sk-your-openai-api-key-here

# Upstash Redis (for rate limiting)
# Get these from: https://console.upstash.com/
# IMPORTANT: Use REST API credentials, not the regular Redis URL
# UPSTASH_REDIS_REST_URL=https://your-redis-instance.upstash.io
# UPSTASH_REDIS_REST_TOKEN=your-upstash-rest-token

# Sentry (for error tracking)
# Get your DSN from: https://sentry.io/
# SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id
# SENTRY_AUTH_TOKEN=your-sentry-auth-token

# ----------------
# DEVELOPMENT
# ----------------

# Node Environment
NODE_ENV=development

# Next.js Configuration
# NEXT_PUBLIC_APP_URL=http://localhost:3000

# ----------------
# NOTES
# ----------------

# 1. Copy this file to .env.local and fill in your values
# 2. Never commit .env.local to git
# 3. All NEXT_PUBLIC_* variables are exposed to the browser
# 4. Server-only variables (like ENCRYPTION_KEY) are never exposed to the client
